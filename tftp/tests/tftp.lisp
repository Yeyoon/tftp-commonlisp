(defpackage tftp-test
  (:use :cl
        :tftp
        :prove))
(in-package :tftp-test)

;; NOTE: To run this test file, execute `(asdf:test-system :tftp)' in your Lisp.

(plan nil)

;; blah blah blah.

(finalize)
